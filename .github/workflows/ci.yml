name: DevOps Task API CI/CD

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: npm install

      # 🔐 4️⃣ SAST - Static Application Security Testing
      - name: Run SAST (npm audit)
        run: npm audit --audit-level=high

      # 5️⃣ Run tests
      - name: Run Jest tests
        run: npm test

      # 6️⃣ Lint code
      - name: Lint code
        run: |
          npm install eslint
          npx eslint index.js || echo "Lint warnings only"
<<<<<<< HEAD
      # 6️⃣ Build Docker image
=======

      # ▶️ 7️⃣ Start API (required for DAST)
      - name: Start API
        run: |
          node index.js &
          sleep 10

      # 🔎 8️⃣ DAST - OWASP ZAP
      - name: Run DAST scan
        uses: zaproxy/action-baseline@v0.9.0
        with:
          target: http://localhost:3000

      # 9️⃣ Build Docker image
>>>>>>> 42fac38 (add sast and dast security checks)
      - name: Build Docker image
        run: docker build -t devops-task-api .

      # 🔟 Save logs
      - name: Save build logs
        run: |
          echo "Build completed at $(date)" > build.log
          cat build.log