apiVersion: v1
kind: Service
metadata:
  name: devops-task-api-service
spec:
  type: NodePort
  selector:
    app: devops-task-api
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007
